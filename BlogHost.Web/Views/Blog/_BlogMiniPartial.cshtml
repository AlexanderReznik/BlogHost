@model BlogHost.Domain.Entities.Blog

<div class="item">
    <h3>@Html.ActionLink(Model.Name, "InBlog", "Post", new { blogid = Model.id, blogname = Model.Name, useremail = Model.User.Email }, null)</h3>
    Owner : @Model.User.Username
</div>